/* =========================================================
   LabLedger — Frontend configuration
   ---------------------------------------------------------
   This file is the frontend equivalent of a .env file.
   Edit the values below to match your backend deployment.

   For local development this is normally:
     API_BASE_URL = 'http://localhost:3000'

   For production set it to your deployed backend URL, e.g.:
     API_BASE_URL = 'https://labledger.example.com'

   Load order: this script MUST be loaded before js/api.js
   so that the constant is available when api.js executes.
   ========================================================= */

const API_BASE_URL = 'https://lab-overall-ledger.onrender.com';
